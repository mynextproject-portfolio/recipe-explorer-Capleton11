global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "recipe-explorer"
    static_configs:
      - targets: ["app:8000"]   # Docker service name — resolves inside the Docker network
    metrics_path: /metrics
    scrape_interval: 15s
